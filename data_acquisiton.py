# get all the required data

import requests
import pandas as pd
import time

config_path = "config.yaml"

def __init__(self, config_path):
    # Load config# Set API endpoints
    return

def download_expression_data(self, cancer_type):
    # Use TCGA API to get HTSeq counts# Save to data/raw/
    return

def download_clinical_data(self, cancer_type):
    # Get survival, stage, demographics# Save to data/raw/
    return

def download_sample_metadata(self, cancer_type):
    # Get tumor vs normal labels# Save to data/metadata/
    return